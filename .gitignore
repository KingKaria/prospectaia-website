# Generated site output — built from pages/ + partials/ via build.py
# Run `python build.py` locally to regenerate these for preview.
# CI (.github/workflows/deploy.yml) builds and deploys fresh on every push.
/*.html
